<div>
  <div class="logout-button">
  <%= form_tag(destroy_user_session_path, method: :delete) do %>
    <%= submit_tag 'Cerrar Sesión' %>
  <% end %>
  <div class="delete-user-button">
  <%= form_tag(user_registration_path, method: :delete, data: { confirm: '¿Estás seguro que deseas eliminar tu cuenta? Esta acción no se puede deshacer.' }) do %>
    <%= submit_tag 'Eliminar Usuario' %>
  <% end %>
</div>
  </div>
</div>
<div class="page-title">Enlaces mas visitados</div>
  <div class="link-group">
    <h2 class="title"><%= link_to 'Regulares', regular_links_path %></h2>
    <div class="link-buttons">
    <button><%= link_to 'Nuevo', new_regular_link_path(new_go_back: root_path) %></button>
    </div>
    <%= render_links(@regular_links) %>
  </div>

  <div class="link-group">
    <h2 class="title"><%= link_to 'Temporales', temporal_links_path %></h2>
    <div class="link-buttons">
    <button><%= link_to 'Nuevo', new_temporal_link_path(new_go_back: root_path) %></button>
    </div>
    <%= render_links(@temporal_links) %>
  </div>

  <div class="link-group">
    <h2 class="title"><%= link_to 'Privados', private_links_path %></h2>
    <div class="link-buttons">
      <button><%= link_to 'Nuevo', new_private_link_path(new_go_back: root_path) %></button>
    </div>
    <%= render_links(@private_links) %>
  </div>

  <div class="link-group">
    <h2 class="title"><%= link_to 'Efímeros', ephemeral_links_path %></h2>
    <div class="link-buttons">
    <button><%= link_to 'Nuevo', new_ephemeral_link_path(new_go_back: root_path) %></button>
    </div>
    <%= render_links(@ephemeral_links) %>
  </div>
</div>
