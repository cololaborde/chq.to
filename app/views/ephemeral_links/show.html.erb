<p style="color: green"><%= notice %></p>

<%= render @ephemeral_link %>

<div>
  <%= link_to "Editar", edit_ephemeral_link_path(@ephemeral_link) %> |
  <%= link_to "Links efímeros", ephemeral_links_path %>

  <%= button_to "Borrar", @ephemeral_link, method: :delete %>
</div>
