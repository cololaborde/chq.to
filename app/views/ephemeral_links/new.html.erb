<h1>New ephemeral link</h1>

<%= form_with(model: @ephemeral_link) do |form| %>
  <%= render 'links/form', form: form %>
<% end %>

<br>

<div>
  <%= link_to "Back to ephemeral links", ephemeral_links_path %>
</div>
